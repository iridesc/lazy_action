services:
  redis:
    image: docker.io/library/redis:latest
    container_name: lazy_action_redis
    ports:
      - "6379:6379"
    command: redis-server --requirepass passwd
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data: